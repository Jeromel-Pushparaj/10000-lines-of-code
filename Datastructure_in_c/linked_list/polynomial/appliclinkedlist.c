#include<stdio.h>
#include<stdlib.h>

//create singly linked list polynomial node
struct node {
    float coefficient;
    int exponent;
    struct node *link;
};

int main(){
    struct node* head = NULL;
    printf("Enter the polynomial\n ");
    
}